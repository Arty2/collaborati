{
  "name": "collaborati",
  "version": "0.82.0",
  "private": true,
  "description": "Local-first, file-native kanban board — single-file HTML app built from src/.",
  "homepage": "https://github.com/arty2/collaborati",
  "license": "MIT",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js --check",
    "test": "playwright test",
    "test:integrity": "node --test tests/integrity.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
